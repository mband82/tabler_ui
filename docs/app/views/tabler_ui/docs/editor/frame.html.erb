<%# locals: none. Rendered under layouts/tabler_ui/docs/editor_frame (see
    that layout's own header comment) -- deliberately empty. The canvas div
    the layout wraps this in is populated by the editor's own JS, not by
    anything server-rendered here. %>
