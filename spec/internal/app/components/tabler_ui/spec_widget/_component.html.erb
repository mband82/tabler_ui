<%# Throwaway fixture partial for TablerUi::Ui modern-path dispatch specs (see
    spec/lib/tabler_ui/ui_spec.rb). Content is irrelevant to those specs --
    they capture the component instance directly via a closure -- so this
    just needs to render without raising. %>
<div class="spec-widget"><%= spec_widget.icon %></div>
